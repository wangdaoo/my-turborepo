import * as React from "react";

interface Props {
  children?: React.ReactNode;
}

export const {{ pascalCase name }} = ({ children }: Props) => {
  return (
    <div>
      <h2>{{ name }}</h2>
      {children}
    </div>
  );
};
